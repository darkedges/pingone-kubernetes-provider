---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: pingaccesses.pingone.io
spec:
  group: pingone.io
  names:
    kind: PingAccess
    listKind: PingAccessList
    plural: pingaccesses
    singular: pingaccess
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.environmentRef
      name: Environment
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PingAccess is the Schema for the pingaccesses API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PingAccessSpec defines the desired state of a PingAccess
              deployment.
            properties:
              adminIngress:
                description: |-
                  AdminIngress configures the Kubernetes Ingress for the PingAccess admin console.
                  Hostname defaults to pa-admin.<spec.domain> when spec.domain is set.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are merged on top of the global annotations
                      for this component.
                    type: object
                  className:
                    description: ClassName overrides the global ingressClassName for
                      this component.
                    type: string
                  enabled:
                    description: |-
                      Enabled controls whether an Ingress resource is created for this component.
                      When nil the value is inherited from spec.ingress.enabled.
                    type: boolean
                  hostname:
                    description: Hostname is the FQDN for this component. Defaults
                      to <prefix>.<spec.domain> when spec.domain is set.
                    type: string
                  tlsSecretRef:
                    description: TLSSecretRef overrides the global TLS secret name
                      for this component.
                    type: string
                type: object
              config:
                description: Config holds PingAccess-specific environment variable
                  configuration.
                properties:
                  adminPort:
                    description: 'AdminPort is the HTTPS port for the PingAccess admin
                      console (PA_ADMIN_PORT). Default: 9000.'
                    format: int32
                    type: integer
                  adminPublicHostname:
                    description: AdminPublicHostname is the public hostname of the
                      PA admin node (PA_ADMIN_PUBLIC_HOSTNAME).
                    type: string
                  adminSecretRef:
                    description: AdminSecretRef is the name of a Secret containing
                      admin credentials.
                    type: string
                  enginePort:
                    description: 'EnginePort is the HTTPS port for the PingAccess
                      engine (PA_ENGINE_PORT). Default: 3000.'
                    format: int32
                    type: integer
                  enginePublicHostname:
                    description: EnginePublicHostname is the public hostname of the
                      PA engine node (PA_ENGINE_PUBLIC_HOSTNAME).
                    type: string
                  envConfigMapRef:
                    description: EnvConfigMapRef is the name of a ConfigMap with additional
                      env vars to inject.
                    type: string
                  fipsModeOn:
                    description: 'FIPSModeOn enables FIPS mode (FIPS_MODE_ON). Default:
                      false.'
                    type: boolean
                  javaRamPercentage:
                    description: 'JavaRAMPercentage is the percentage of container
                      memory for the JVM (JAVA_RAM_PERCENTAGE). Default: 60.0.'
                    type: string
                  operationalMode:
                    description: 'OperationalMode is one of STANDALONE, CLUSTERED_CONSOLE,
                      CLUSTERED_ENGINE (OPERATIONAL_MODE). Default: STANDALONE.'
                    type: string
                  serverProfile:
                    description: ServerProfile is the primary server profile for PingAccess.
                    properties:
                      branch:
                        description: Branch is the Git branch to check out. Omit to
                          use the repo default branch.
                        type: string
                      parent:
                        description: |-
                          Parent is the name of a layer in serverProfileLayers that is applied beneath this profile.
                          Sets SERVER_PROFILE_PARENT.
                        type: string
                      path:
                        description: Path is the subdirectory within the git repo.
                        type: string
                      url:
                        description: URL is the Git HTTPS URL of the server profile
                          repo.
                        type: string
                    type: object
                  serverProfileLayers:
                    description: |-
                      ServerProfileLayers defines additional named profile layers (layered profiles).
                      Keys are the layer names (e.g. "BASE"); env vars use the uppercased key.
                    items:
                      description: |-
                        ServerProfileLayerSpec defines a named server profile layer for layered profiles.
                        Each entry maps to SERVER_PROFILE_<UPPER(NAME)>_URL / _BRANCH / _PATH / _PARENT.
                      properties:
                        branch:
                          description: Branch is the Git branch to check out.
                          type: string
                        name:
                          description: |-
                            Name is the layer identifier (e.g. paz, base, extensions).
                            Env vars use the uppercased name: paz → SERVER_PROFILE_PAZ_URL etc.
                          type: string
                        parent:
                          description: Parent is the name of a deeper layer in serverProfileLayers
                            (sets SERVER_PROFILE_<NAME>_PARENT).
                          type: string
                        path:
                          description: Path is the subdirectory within the git repo.
                          type: string
                        url:
                          description: URL is the Git HTTPS URL of this layer's repo.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                type: object
              container:
                description: Container holds container-level settings such as service
                  wait conditions.
                properties:
                  includeVolumes:
                    description: |-
                      IncludeVolumes is a list of volume names defined in PingEnvironment.spec.volumes
                      to mount into this product's pod.
                    items:
                      type: string
                    type: array
                  waitFor:
                    description: WaitFor is a list of services this container waits
                      for before starting.
                    items:
                      description: WaitForSpec defines a single service dependency
                        that must be reachable before the container starts.
                      properties:
                        application:
                          description: |-
                            Application is the logical name of the Ping product to wait for.
                            Supported values: pingDirectory, pingFederate, pingFederateAdmin, pingFederateEngine,
                            pingAccess, pingAccessAdmin, pingAccessEngine, pingAuthorize, pingAuthorizePAP, pingDataConsole.
                            The operator resolves this to the correct Helm sub-chart service name automatically.
                          type: string
                        service:
                          description: Service is the port/service type to probe (e.g.
                            ldaps, https, ldap).
                          type: string
                        timeoutSeconds:
                          description: 'TimeoutSeconds is how long to wait before
                            giving up. Default: 300.'
                          format: int32
                          type: integer
                      required:
                      - application
                      - service
                      type: object
                    type: array
                type: object
              engineIngress:
                description: |-
                  EngineIngress configures the Kubernetes Ingress for the PingAccess engine.
                  Hostname defaults to pa.<spec.domain> when spec.domain is set.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are merged on top of the global annotations
                      for this component.
                    type: object
                  className:
                    description: ClassName overrides the global ingressClassName for
                      this component.
                    type: string
                  enabled:
                    description: |-
                      Enabled controls whether an Ingress resource is created for this component.
                      When nil the value is inherited from spec.ingress.enabled.
                    type: boolean
                  hostname:
                    description: Hostname is the FQDN for this component. Defaults
                      to <prefix>.<spec.domain> when spec.domain is set.
                    type: string
                  tlsSecretRef:
                    description: TLSSecretRef overrides the global TLS secret name
                      for this component.
                    type: string
                type: object
              environmentRef:
                description: EnvironmentRef is the name of the PingEnvironment CR
                  this product belongs to.
                type: string
              image:
                description: Image is the container image repository. Omit to use
                  the chart's default.
                type: string
              replicas:
                description: 'Replicas is the desired number of PingAccess engine
                  pods. Default: 1.'
                format: int32
                type: integer
              valuesOverride:
                description: ValuesOverride is merged on top of the base Helm values
                  as raw JSON.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              version:
                description: Version is the container image tag or full image reference
                  (e.g. 8.1.0-edge).
                type: string
            type: object
          status:
            description: PingAccessStatus describes the observed state of a PingAccess
              resource.
            properties:
              conditions:
                description: Conditions holds the latest available observations of
                  the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the generation last processed by
                  the reconciler.
                format: int64
                type: integer
              phase:
                description: 'Phase is the current lifecycle phase: Pending, Ready,
                  or Failed.'
                type: string
              release:
                description: Release is the Helm release name that manages this product.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
