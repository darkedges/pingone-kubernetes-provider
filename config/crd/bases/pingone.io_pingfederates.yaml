---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: pingfederates.pingone.io
spec:
  group: pingone.io
  names:
    kind: PingFederate
    listKind: PingFederateList
    plural: pingfederates
    singular: pingfederate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.environmentRef
      name: Environment
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PingFederate is the Schema for the pingfederates API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PingFederateSpec defines the desired state of a PingFederate
              deployment.
            properties:
              adminIngress:
                description: |-
                  AdminIngress configures the Kubernetes Ingress for the PingFederate admin console.
                  Hostname defaults to pf-admin.<spec.domain> when spec.domain is set.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are merged on top of the global annotations
                      for this component.
                    type: object
                  className:
                    description: ClassName overrides the global ingressClassName for
                      this component.
                    type: string
                  enabled:
                    description: |-
                      Enabled controls whether an Ingress resource is created for this component.
                      When nil the value is inherited from spec.ingress.enabled.
                    type: boolean
                  hostname:
                    description: Hostname is the FQDN for this component. Defaults
                      to <prefix>.<spec.domain> when spec.domain is set.
                    type: string
                  tlsSecretRef:
                    description: TLSSecretRef overrides the global TLS secret name
                      for this component.
                    type: string
                type: object
              config:
                description: Config holds PingFederate-specific environment variable
                  configuration.
                properties:
                  acceptQueueSize:
                    description: 'AcceptQueueSize sets the size of the Jetty accept
                      queue (PF_ACCEPT_QUEUE_SIZE). Default: 512.'
                    format: int32
                    type: integer
                  adminAPIAuthentication:
                    description: 'AdminAPIAuthentication is the admin API auth mechanism
                      (PF_ADMIN_API_AUTHENTICATION). Default: native.'
                    type: string
                  adminDebug:
                    description: 'AdminDebug enables Java remote debugging on the
                      PingFederate admin (PF_ADMIN_DEBUG). Default: false.'
                    type: boolean
                  adminPort:
                    description: 'AdminPort is the HTTPS port for the PingFederate
                      admin console/API (PF_ADMIN_PORT). Default: 9999.'
                    format: int32
                    type: integer
                  adminPublicBaseURL:
                    description: AdminPublicBaseURL is the full base URL of the PF
                      admin console (PF_ADMIN_PUBLIC_BASEURL).
                    type: string
                  adminPublicHostname:
                    description: AdminPublicHostname is the public hostname of the
                      PF admin node (PF_ADMIN_PUBLIC_HOSTNAME).
                    type: string
                  adminSecretRef:
                    description: AdminSecretRef is the name of a Secret containing
                      admin credentials.
                    type: string
                  adminWaitForTimeout:
                    description: 'AdminWaitForTimeout is how long (seconds) to wait
                      for the admin API before failing (ADMIN_WAITFOR_TIMEOUT). Default:
                      300.'
                    format: int32
                    type: integer
                  bcFIPSApprovedOnly:
                    description: 'BCFIPSApprovedOnly restricts the JVM to FIPS-approved
                      algorithms only (PF_BC_FIPS_APPROVED_ONLY). Default: false.'
                    type: boolean
                  consoleAuthentication:
                    description: 'ConsoleAuthentication is the admin console auth
                      mechanism (PF_CONSOLE_AUTHENTICATION). Default: native.'
                    type: string
                  consoleEnvironment:
                    description: ConsoleEnvironment is the environment label shown
                      in the admin console UI (PF_CONSOLE_ENV).
                    type: string
                  consoleTitle:
                    description: ConsoleTitle is the title shown in the admin console
                      (PF_CONSOLE_TITLE).
                    type: string
                  createInitialAdminUser:
                    description: 'CreateInitialAdminUser creates the initial administrator
                      account after first startup (CREATE_INITIAL_ADMIN_USER). Default:
                      false.'
                    type: boolean
                  debugPort:
                    description: |-
                      DebugPort is the Java remote debug listener port (PF_DEBUG_PORT). Default: 9030.
                      Only meaningful when engineDebug or adminDebug is true.
                    format: int32
                    type: integer
                  enableAutomaticHeapDump:
                    description: |-
                      EnableAutomaticHeapDump enables JVM heap dumps on OutOfMemoryError (ENABLE_AUTOMATIC_HEAP_DUMP). Default: true.
                      Set to false to disable automatic heap dumps.
                    type: boolean
                  engineDebug:
                    description: 'EngineDebug enables Java remote debugging on the
                      PingFederate engine (PF_ENGINE_DEBUG). Default: false.'
                    type: boolean
                  enginePort:
                    description: 'EnginePort is the HTTPS port for the PingFederate
                      runtime engine (PF_ENGINE_PORT). Default: 9031.'
                    format: int32
                    type: integer
                  enginePublicHostname:
                    description: EnginePublicHostname is the public hostname of the
                      PF engine node (PF_ENGINE_PUBLIC_HOSTNAME).
                    type: string
                  engineSecondaryPort:
                    description: 'EngineSecondaryPort is a secondary HTTPS port for
                      mutual SSL/TLS client certificate authentication (PF_ENGINE_SECONDARY_PORT).
                      Default: -1 (disabled).'
                    format: int32
                    type: integer
                  envConfigMapRef:
                    description: EnvConfigMapRef is the name of a ConfigMap with additional
                      env vars to inject.
                    type: string
                  envs:
                    additionalProperties:
                      type: string
                    description: |-
                      Envs is a map of additional environment variables to inject into this product's container.
                      Keys override any built-in env vars with the same name.
                    type: object
                  hsmHybrid:
                    description: 'HSMHybrid enables HSM hybrid mode where certs/keys
                      can be created on the local trust store or the HSM (PF_HSM_HYBRID).
                      Default: false.'
                    type: boolean
                  hsmMode:
                    description: 'HSMMode is the Hardware Security Module mode (HSM_MODE).
                      Default: OFF.'
                    type: string
                  javaRamPercentage:
                    description: 'JavaRAMPercentage is the percentage of container
                      memory for the JVM (JAVA_RAM_PERCENTAGE). Default: 75.0.'
                    type: string
                  jettyThreadsMax:
                    description: JettyThreadsMax overrides the maximum Jetty thread
                      pool size (PF_JETTY_THREADS_MAX).
                    format: int32
                    type: integer
                  jettyThreadsMin:
                    description: JettyThreadsMin overrides the minimum Jetty thread
                      pool size (PF_JETTY_THREADS_MIN).
                    format: int32
                    type: integer
                  ldapSecretRef:
                    description: LDAPSecretRef is the name of a Secret containing
                      PF_LDAP_PASSWORD.
                    type: string
                  ldapType:
                    description: 'LDAPType is the LDAP directory type when using LDAP
                      auth (PF_LDAP_TYPE). Default: PingDirectory.'
                    type: string
                  ldapUsername:
                    description: LDAPUsername is the username for LDAP lookups (PF_LDAP_USERNAME).
                    type: string
                  logNumber:
                    description: 'LogNumber is the maximum number of log files retained
                      on rotation (PF_LOG_NUMBER). Default: 2.'
                    format: int32
                    type: integer
                  logSizeMax:
                    description: 'LogSizeMax is the maximum size for all log file
                      appenders (PF_LOG_SIZE_MAX). Default: "10000 KB".'
                    type: string
                  nodeTags:
                    description: NodeTags are space-separated tags associated with
                      this PingFederate node used for request routing (PF_NODE_TAGS).
                    type: string
                  operationalMode:
                    description: 'OperationalMode is one of STANDALONE, CLUSTERED_CONSOLE,
                      CLUSTERED_ENGINE (OPERATIONAL_MODE). Default: STANDALONE.'
                    type: string
                  pingOneEnvID:
                    description: PingOneEnvID is the PingOne environment ID to connect
                      to (PF_PINGONE_ENV_ID).
                    type: string
                  pingOneRegion:
                    description: PingOneRegion is the region of the PingOne tenant
                      (PF_PINGONE_REGION).
                    type: string
                  provisionerGracePeriod:
                    description: |-
                      ProvisionerGracePeriod is the provisioner failover grace period in seconds (PF_PROVISIONER_GRACE_PERIOD). Default: 600.
                      Only meaningful when provisionerMode is FAILOVER.
                    format: int32
                    type: integer
                  provisionerMode:
                    description: 'ProvisionerMode is one of OFF, STANDALONE, FAILOVER
                      (PF_PROVISIONER_MODE). Default: OFF.'
                    type: string
                  provisionerNodeID:
                    description: 'ProvisionerNodeID is the provisioner node ID (PF_PROVISIONER_NODE_ID).
                      Default: 1.'
                    format: int32
                    type: integer
                  serverProfile:
                    description: ServerProfile is the primary server profile for PingFederate.
                    properties:
                      branch:
                        description: Branch is the Git branch to check out. Omit to
                          use the repo default branch.
                        type: string
                      parent:
                        description: |-
                          Parent is the name of a layer in serverProfileLayers that is applied beneath this profile.
                          Sets SERVER_PROFILE_PARENT.
                        type: string
                      path:
                        description: Path is the subdirectory within the git repo.
                        type: string
                      url:
                        description: URL is the Git HTTPS URL of the server profile
                          repo.
                        type: string
                    type: object
                  serverProfileLayers:
                    description: |-
                      ServerProfileLayers defines additional named profile layers (layered profiles).
                      Keys are the layer names; env vars use the uppercased key.
                    items:
                      description: |-
                        ServerProfileLayerSpec defines a named server profile layer for layered profiles.
                        Each entry maps to SERVER_PROFILE_<UPPER(NAME)>_URL / _BRANCH / _PATH / _PARENT.
                      properties:
                        branch:
                          description: Branch is the Git branch to check out.
                          type: string
                        name:
                          description: |-
                            Name is the layer identifier (e.g. paz, base, extensions).
                            Env vars use the uppercased name: paz → SERVER_PROFILE_PAZ_URL etc.
                          type: string
                        parent:
                          description: Parent is the name of a deeper layer in serverProfileLayers
                            (sets SERVER_PROFILE_<NAME>_PARENT).
                          type: string
                        path:
                          description: Path is the subdirectory within the git repo.
                          type: string
                        url:
                          description: URL is the Git HTTPS URL of this layer's repo.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                type: object
              container:
                description: Container holds container-level settings such as service
                  wait conditions.
                properties:
                  containerSecurityContext:
                    description: ContainerSecurityContext overrides the container-level
                      securityContext for this product.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  includeVolumes:
                    description: |-
                      IncludeVolumes is a list of volume names defined in PingEnvironment.spec.volumes
                      to mount into this product's pod.
                    items:
                      type: string
                    type: array
                  resources:
                    description: |-
                      Resources overrides the CPU/memory requests and limits for this product's containers.
                      Takes precedence over the tier-based defaults and PingEnvironment.spec.resources.
                    properties:
                      limits:
                        additionalProperties:
                          type: string
                        description: Limits are the maximum resources the container
                          may use.
                        type: object
                      requests:
                        additionalProperties:
                          type: string
                        description: Requests are the minimum resources the container
                          requires.
                        type: object
                    type: object
                  securityContext:
                    description: SecurityContext overrides the pod-level securityContext
                      for this product's workload.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  volumeMounts:
                    description: |-
                      VolumeMounts is a list of additional container volumeMounts for this product's workload.
                      Each element is a standard Kubernetes volumeMount spec (name, mountPath, subPath, etc.).
                      Maps directly to the ping-devops sub-chart volumeMounts array.
                    items:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  volumes:
                    description: |-
                      Volumes is a list of additional pod-level volumes for this product's workload.
                      Each element is a standard Kubernetes volume spec (name, configMap, secret, emptyDir, etc.).
                      Maps directly to the ping-devops sub-chart volumes array.
                    items:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  waitFor:
                    description: WaitFor is a list of services this container waits
                      for before starting.
                    items:
                      description: WaitForSpec defines a single service dependency
                        that must be reachable before the container starts.
                      properties:
                        application:
                          description: |-
                            Application is the logical name of the Ping product to wait for.
                            Supported values: pingDirectory, pingFederate, pingFederateAdmin, pingFederateEngine,
                            pingAccess, pingAccessAdmin, pingAccessEngine, pingAuthorize, pingAuthorizePAP, pingDataConsole.
                            The operator resolves this to the correct Helm sub-chart service name automatically.
                          type: string
                        service:
                          description: Service is the port/service type to probe (e.g.
                            ldaps, https, ldap).
                          type: string
                        timeoutSeconds:
                          description: 'TimeoutSeconds is how long to wait before
                            giving up. Default: 300.'
                          format: int32
                          type: integer
                      required:
                      - application
                      - service
                      type: object
                    type: array
                type: object
              engineIngress:
                description: |-
                  EngineIngress configures the Kubernetes Ingress for the PingFederate runtime engine.
                  Hostname defaults to pf.<spec.domain> when spec.domain is set.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are merged on top of the global annotations
                      for this component.
                    type: object
                  className:
                    description: ClassName overrides the global ingressClassName for
                      this component.
                    type: string
                  enabled:
                    description: |-
                      Enabled controls whether an Ingress resource is created for this component.
                      When nil the value is inherited from spec.ingress.enabled.
                    type: boolean
                  hostname:
                    description: Hostname is the FQDN for this component. Defaults
                      to <prefix>.<spec.domain> when spec.domain is set.
                    type: string
                  tlsSecretRef:
                    description: TLSSecretRef overrides the global TLS secret name
                      for this component.
                    type: string
                type: object
              environmentRef:
                description: EnvironmentRef is the name of the PingEnvironment CR
                  this product belongs to.
                type: string
              image:
                description: |-
                  Image is the container image repository (e.g. registry.example.com/org/pingfederate).
                  Omit to use the chart's default image repository.
                type: string
              replicas:
                description: 'Replicas is the desired number of PingFederate engine
                  pods. Default: 1.'
                format: int32
                type: integer
              service:
                description: |-
                  Service holds Kubernetes Service customisation for this product.
                  Annotations are merged on top of spec.services.annotations.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are merged on top of the global service
                      annotations for this component.
                    type: object
                type: object
              valuesOverride:
                description: ValuesOverride is merged on top of the base Helm values
                  as raw JSON.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              version:
                description: |-
                  Version is the container image tag (e.g. 13.0.2-edge).
                  Omit to use the chart's default tag.
                type: string
            type: object
          status:
            description: PingFederateStatus describes the observed state of a PingFederate
              resource.
            properties:
              conditions:
                description: Conditions holds the latest available observations of
                  the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the generation last processed by
                  the reconciler.
                format: int64
                type: integer
              phase:
                description: 'Phase is the current lifecycle phase: Pending, Ready,
                  or Failed.'
                type: string
              release:
                description: Release is the Helm release name that manages this product.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
