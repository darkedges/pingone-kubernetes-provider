---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: pingauthorizes.pingone.io
spec:
  group: pingone.io
  names:
    kind: PingAuthorize
    listKind: PingAuthorizeList
    plural: pingauthorizes
    singular: pingauthorize
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.environmentRef
      name: Environment
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PingAuthorize is the Schema for the pingauthorizes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PingAuthorizeSpec defines the desired state of a PingAuthorize
              deployment.
            properties:
              config:
                description: Config holds PingAuthorize-specific environment variable
                  configuration.
                properties:
                  adminSecretRef:
                    description: AdminSecretRef is the name of a Secret containing
                      the root user password (ROOT_USER_PASSWORD_FILE).
                    type: string
                  adminUserName:
                    description: 'AdminUserName is the admin user (ADMIN_USER_NAME).
                      Default: admin.'
                    type: string
                  encryptionSecretRef:
                    description: EncryptionSecretRef is the name of a Secret containing
                      the encryption passphrase (ENCRYPTION_PASSWORD_FILE).
                    type: string
                  envConfigMapRef:
                    description: EnvConfigMapRef is the name of a ConfigMap with additional
                      env vars to inject.
                    type: string
                  envs:
                    additionalProperties:
                      type: string
                    description: |-
                      Envs is a map of additional environment variables to inject into this product's container.
                      Keys override any built-in env vars with the same name.
                    type: object
                  httpsPort:
                    description: 'HTTPSPort is the container HTTPS port (HTTPS_PORT).
                      Default: 1443.'
                    format: int32
                    type: integer
                  ldapPort:
                    description: 'LDAPPort is the container LDAP port (LDAP_PORT).
                      Default: 1389.'
                    format: int32
                    type: integer
                  ldapsPort:
                    description: 'LDAPSPort is the container LDAPS port (LDAPS_PORT).
                      Default: 1636.'
                    format: int32
                    type: integer
                  maxHeapSize:
                    description: 'MaxHeapSize is the JVM maximum heap size (MAX_HEAP_SIZE).
                      Default: 1g.'
                    type: string
                  retryTimeoutSeconds:
                    description: 'RetryTimeoutSeconds is the timeout for startup operations
                      (RETRY_TIMEOUT_SECONDS). Default: 180.'
                    format: int32
                    type: integer
                  serverProfile:
                    description: ServerProfile is the primary server profile for PingAuthorize.
                    properties:
                      branch:
                        description: Branch is the Git branch to check out. Omit to
                          use the repo default branch.
                        type: string
                      parent:
                        description: |-
                          Parent is the name of a layer in serverProfileLayers that is applied beneath this profile.
                          Sets SERVER_PROFILE_PARENT.
                        type: string
                      path:
                        description: Path is the subdirectory within the git repo.
                        type: string
                      url:
                        description: URL is the Git HTTPS URL of the server profile
                          repo.
                        type: string
                    type: object
                  serverProfileLayers:
                    description: |-
                      ServerProfileLayers defines additional named profile layers (layered profiles).
                      Keys are the layer names (e.g. "PAZ", "BASE"); env vars use the uppercased key.
                    items:
                      description: |-
                        ServerProfileLayerSpec defines a named server profile layer for layered profiles.
                        Each entry maps to SERVER_PROFILE_<UPPER(NAME)>_URL / _BRANCH / _PATH / _PARENT.
                      properties:
                        branch:
                          description: Branch is the Git branch to check out.
                          type: string
                        name:
                          description: |-
                            Name is the layer identifier (e.g. paz, base, extensions).
                            Env vars use the uppercased name: paz → SERVER_PROFILE_PAZ_URL etc.
                          type: string
                        parent:
                          description: Parent is the name of a deeper layer in serverProfileLayers
                            (sets SERVER_PROFILE_<NAME>_PARENT).
                          type: string
                        path:
                          description: Path is the subdirectory within the git repo.
                          type: string
                        url:
                          description: URL is the Git HTTPS URL of this layer's repo.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  userBaseDN:
                    description: 'UserBaseDN is the base DN for user data (USER_BASE_DN).
                      Default: dc=example,dc=com.'
                    type: string
                type: object
              container:
                description: Container holds container-level settings such as service
                  wait conditions.
                properties:
                  containerSecurityContext:
                    description: ContainerSecurityContext overrides the container-level
                      securityContext for this product.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  includeVolumes:
                    description: |-
                      IncludeVolumes is a list of volume names defined in PingEnvironment.spec.volumes
                      to mount into this product's pod.
                    items:
                      type: string
                    type: array
                  resources:
                    description: |-
                      Resources overrides the CPU/memory requests and limits for this product's containers.
                      Takes precedence over the tier-based defaults and PingEnvironment.spec.resources.
                    properties:
                      limits:
                        additionalProperties:
                          type: string
                        description: Limits are the maximum resources the container
                          may use.
                        type: object
                      requests:
                        additionalProperties:
                          type: string
                        description: Requests are the minimum resources the container
                          requires.
                        type: object
                    type: object
                  securityContext:
                    description: SecurityContext overrides the pod-level securityContext
                      for this product's workload.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  volumeMounts:
                    description: |-
                      VolumeMounts is a list of additional container volumeMounts for this product's workload.
                      Each element is a standard Kubernetes volumeMount spec (name, mountPath, subPath, etc.).
                      Maps directly to the ping-devops sub-chart volumeMounts array.
                    items:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  volumes:
                    description: |-
                      Volumes is a list of additional pod-level volumes for this product's workload.
                      Each element is a standard Kubernetes volume spec (name, configMap, secret, emptyDir, etc.).
                      Maps directly to the ping-devops sub-chart volumes array.
                    items:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  waitFor:
                    description: WaitFor is a list of services this container waits
                      for before starting.
                    items:
                      description: WaitForSpec defines a single service dependency
                        that must be reachable before the container starts.
                      properties:
                        application:
                          description: |-
                            Application is the logical name of the Ping product to wait for.
                            Supported values: pingDirectory, pingFederate, pingFederateAdmin, pingFederateEngine,
                            pingAccess, pingAccessAdmin, pingAccessEngine, pingAuthorize, pingAuthorizePAP, pingDataConsole.
                            The operator resolves this to the correct Helm sub-chart service name automatically.
                          type: string
                        service:
                          description: Service is the port/service type to probe (e.g.
                            ldaps, https, ldap).
                          type: string
                        timeoutSeconds:
                          description: 'TimeoutSeconds is how long to wait before
                            giving up. Default: 300.'
                          format: int32
                          type: integer
                      required:
                      - application
                      - service
                      type: object
                    type: array
                type: object
              environmentRef:
                description: EnvironmentRef is the name of the PingEnvironment CR
                  this product belongs to.
                type: string
              image:
                description: Image is the container image repository. Omit to use
                  the chart's default.
                type: string
              ingress:
                description: |-
                  Ingress configures the Kubernetes Ingress for the PingAuthorize management interface.
                  Hostname defaults to paz.<spec.domain> when spec.domain is set.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are merged on top of the global annotations
                      for this component.
                    type: object
                  className:
                    description: ClassName overrides the global ingressClassName for
                      this component.
                    type: string
                  enabled:
                    description: |-
                      Enabled controls whether an Ingress resource is created for this component.
                      When nil the value is inherited from spec.ingress.enabled.
                    type: boolean
                  hostname:
                    description: Hostname is the FQDN for this component. Defaults
                      to <prefix>.<spec.domain> when spec.domain is set.
                    type: string
                  tlsSecretRef:
                    description: TLSSecretRef overrides the global TLS secret name
                      for this component.
                    type: string
                type: object
              replicas:
                description: 'Replicas is the desired number of PingAuthorize pods.
                  Default: 1.'
                format: int32
                minimum: 0
                type: integer
              service:
                description: |-
                  Service holds Kubernetes Service customisation for this product.
                  Annotations are merged on top of spec.services.annotations.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are merged on top of the global service
                      annotations for this component.
                    type: object
                type: object
              storageClass:
                description: StorageClass is the storage class used for PersistentVolumeClaims.
                type: string
              storageSize:
                description: 'StorageSize is the size of the /opt/out PVC. Default:
                  8Gi.'
                type: string
              valuesOverride:
                description: ValuesOverride is merged on top of the base Helm values
                  as raw JSON.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              version:
                description: Version is the container image tag or full image reference
                  (e.g. 10.3.0.0-edge).
                type: string
            type: object
          status:
            description: PingAuthorizeStatus describes the observed state of a PingAuthorize
              resource.
            properties:
              conditions:
                description: Conditions holds the latest available observations of
                  the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the generation last processed by
                  the reconciler.
                format: int64
                type: integer
              phase:
                description: 'Phase is the current lifecycle phase: Pending, Ready,
                  or Failed.'
                type: string
              release:
                description: Release is the Helm release name that manages this product.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
