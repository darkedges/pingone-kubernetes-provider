---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: pingenvironments.pingone.io
spec:
  group: pingone.io
  names:
    kind: PingEnvironment
    listKind: PingEnvironmentList
    plural: pingenvironments
    singular: pingenvironment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenantId
      name: Tenant
      type: string
    - jsonPath: .spec.tier
      name: Tier
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PingEnvironment is the Schema for the pingenvironments API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PingEnvironmentSpec defines the desired state of PingEnvironment.
            properties:
              domain:
                description: |-
                  Domain is the base DNS domain for this environment. Component hostnames are derived
                  from this value when not explicitly set (e.g. pf.<domain>, pf-admin.<domain>).
                type: string
              ingress:
                description: |-
                  Ingress holds shared ingress settings inherited by all components.
                  Set enabled: true to turn on ingress for all components at once.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are merged into every component's Ingress
                      resource. Per-component annotations take precedence on conflict.
                    type: object
                  className:
                    description: ClassName is the ingressClassName applied to all
                      component ingresses (e.g. nginx, traefik, alb).
                    type: string
                  enabled:
                    description: |-
                      Enabled turns ingress on for all components by default.
                      Each component can still override with its own enabled field.
                    type: boolean
                  tlsSecretRef:
                    description: TLSSecretRef is the default TLS secret name. Per-component
                      tlsSecretRef overrides this.
                    type: string
                type: object
              pingAccess:
                description: PingAccess holds the optional PingAccess deployment configuration.
                properties:
                  adminIngress:
                    description: |-
                      AdminIngress configures the Kubernetes Ingress for the PingAccess admin console.
                      Hostname defaults to pa-admin.<spec.domain> when spec.domain is set.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are merged on top of the global annotations
                          for this component.
                        type: object
                      className:
                        description: ClassName overrides the global ingressClassName
                          for this component.
                        type: string
                      enabled:
                        description: |-
                          Enabled controls whether an Ingress resource is created for this component.
                          When nil the value is inherited from spec.ingress.enabled.
                        type: boolean
                      hostname:
                        description: Hostname is the FQDN for this component. Defaults
                          to <prefix>.<spec.domain> when spec.domain is set.
                        type: string
                      tlsSecretRef:
                        description: TLSSecretRef overrides the global TLS secret
                          name for this component.
                        type: string
                    type: object
                  config:
                    description: Config holds PingAccess-specific environment variable
                      configuration.
                    properties:
                      adminPort:
                        description: 'AdminPort is the HTTPS port for the PingAccess
                          admin console (PA_ADMIN_PORT). Default: 9000.'
                        format: int32
                        type: integer
                      adminPublicHostname:
                        description: AdminPublicHostname is the public hostname of
                          the PA admin node (PA_ADMIN_PUBLIC_HOSTNAME).
                        type: string
                      adminSecretRef:
                        description: AdminSecretRef is the name of a Secret containing
                          admin credentials.
                        type: string
                      enginePort:
                        description: 'EnginePort is the HTTPS port for the PingAccess
                          engine (PA_ENGINE_PORT). Default: 3000.'
                        format: int32
                        type: integer
                      enginePublicHostname:
                        description: EnginePublicHostname is the public hostname of
                          the PA engine node (PA_ENGINE_PUBLIC_HOSTNAME).
                        type: string
                      envConfigMapRef:
                        description: EnvConfigMapRef is the name of a ConfigMap with
                          additional env vars to inject.
                        type: string
                      fipsModeOn:
                        description: 'FIPSModeOn enables FIPS mode (FIPS_MODE_ON).
                          Default: false.'
                        type: boolean
                      javaRamPercentage:
                        description: 'JavaRAMPercentage is the percentage of container
                          memory for the JVM (JAVA_RAM_PERCENTAGE). Default: 60.0.'
                        type: string
                      operationalMode:
                        description: 'OperationalMode is one of STANDALONE, CLUSTERED_CONSOLE,
                          CLUSTERED_ENGINE (OPERATIONAL_MODE). Default: STANDALONE.'
                        type: string
                      serverProfile:
                        description: ServerProfile is the primary server profile for
                          PingAccess.
                        properties:
                          branch:
                            description: Branch is the Git branch to check out. Omit
                              to use the repo default branch.
                            type: string
                          parent:
                            description: |-
                              Parent is the name of a layer in serverProfileLayers that is applied beneath this profile.
                              Sets SERVER_PROFILE_PARENT.
                            type: string
                          path:
                            description: Path is the subdirectory within the git repo.
                            type: string
                          url:
                            description: URL is the Git HTTPS URL of the server profile
                              repo.
                            type: string
                        type: object
                      serverProfileLayers:
                        description: |-
                          ServerProfileLayers defines additional named profile layers (layered profiles).
                          Keys are the layer names (e.g. "BASE"); env vars use the uppercased key.
                        items:
                          description: |-
                            ServerProfileLayerSpec defines a named server profile layer for layered profiles.
                            Each entry maps to SERVER_PROFILE_<UPPER(NAME)>_URL / _BRANCH / _PATH / _PARENT.
                          properties:
                            branch:
                              description: Branch is the Git branch to check out.
                              type: string
                            name:
                              description: |-
                                Name is the layer identifier (e.g. paz, base, extensions).
                                Env vars use the uppercased name: paz → SERVER_PROFILE_PAZ_URL etc.
                              type: string
                            parent:
                              description: Parent is the name of a deeper layer in
                                serverProfileLayers (sets SERVER_PROFILE_<NAME>_PARENT).
                              type: string
                            path:
                              description: Path is the subdirectory within the git
                                repo.
                              type: string
                            url:
                              description: URL is the Git HTTPS URL of this layer's
                                repo.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                    type: object
                  container:
                    description: Container holds container-level settings such as
                      service wait conditions.
                    properties:
                      waitFor:
                        description: WaitFor is a list of services this container
                          waits for before starting.
                        items:
                          description: WaitForSpec defines a single service dependency
                            that must be reachable before the container starts.
                          properties:
                            application:
                              description: |-
                                Application is the logical name of the Ping product to wait for.
                                Supported values: pingDirectory, pingFederate, pingFederateAdmin, pingFederateEngine,
                                pingAccess, pingAccessAdmin, pingAccessEngine, pingAuthorize, pingAuthorizePAP, pingDataConsole.
                                The operator resolves this to the correct Helm sub-chart service name automatically.
                              type: string
                            service:
                              description: Service is the port/service type to probe
                                (e.g. ldaps, https, ldap).
                              type: string
                            timeoutSeconds:
                              description: 'TimeoutSeconds is how long to wait before
                                giving up. Default: 300.'
                              format: int32
                              type: integer
                          required:
                          - application
                          - service
                          type: object
                        type: array
                    type: object
                  engineIngress:
                    description: |-
                      EngineIngress configures the Kubernetes Ingress for the PingAccess engine.
                      Hostname defaults to pa.<spec.domain> when spec.domain is set.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are merged on top of the global annotations
                          for this component.
                        type: object
                      className:
                        description: ClassName overrides the global ingressClassName
                          for this component.
                        type: string
                      enabled:
                        description: |-
                          Enabled controls whether an Ingress resource is created for this component.
                          When nil the value is inherited from spec.ingress.enabled.
                        type: boolean
                      hostname:
                        description: Hostname is the FQDN for this component. Defaults
                          to <prefix>.<spec.domain> when spec.domain is set.
                        type: string
                      tlsSecretRef:
                        description: TLSSecretRef overrides the global TLS secret
                          name for this component.
                        type: string
                    type: object
                  image:
                    description: Image is the container image repository. Omit to
                      use the chart's default.
                    type: string
                  replicas:
                    description: 'Replicas is the desired number of PingAccess engine
                      pods. Default: 1.'
                    format: int32
                    type: integer
                  valuesOverride:
                    description: ValuesOverride is merged on top of the base Helm
                      values as raw JSON.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  version:
                    description: Version is the container image tag or full image
                      reference (e.g. 8.1.0-edge).
                    type: string
                type: object
              pingAuthorize:
                description: PingAuthorize holds the optional PingAuthorize Policy
                  Decision Point deployment configuration.
                properties:
                  config:
                    description: Config holds PingAuthorize-specific environment variable
                      configuration.
                    properties:
                      adminSecretRef:
                        description: AdminSecretRef is the name of a Secret containing
                          the root user password (ROOT_USER_PASSWORD_FILE).
                        type: string
                      adminUserName:
                        description: 'AdminUserName is the admin user (ADMIN_USER_NAME).
                          Default: admin.'
                        type: string
                      encryptionSecretRef:
                        description: EncryptionSecretRef is the name of a Secret containing
                          the encryption passphrase (ENCRYPTION_PASSWORD_FILE).
                        type: string
                      envConfigMapRef:
                        description: EnvConfigMapRef is the name of a ConfigMap with
                          additional env vars to inject.
                        type: string
                      httpsPort:
                        description: 'HTTPSPort is the container HTTPS port (HTTPS_PORT).
                          Default: 1443.'
                        format: int32
                        type: integer
                      ldapPort:
                        description: 'LDAPPort is the container LDAP port (LDAP_PORT).
                          Default: 1389.'
                        format: int32
                        type: integer
                      ldapsPort:
                        description: 'LDAPSPort is the container LDAPS port (LDAPS_PORT).
                          Default: 1636.'
                        format: int32
                        type: integer
                      maxHeapSize:
                        description: 'MaxHeapSize is the JVM maximum heap size (MAX_HEAP_SIZE).
                          Default: 1g.'
                        type: string
                      retryTimeoutSeconds:
                        description: 'RetryTimeoutSeconds is the timeout for startup
                          operations (RETRY_TIMEOUT_SECONDS). Default: 180.'
                        format: int32
                        type: integer
                      serverProfile:
                        description: ServerProfile is the primary server profile for
                          PingAuthorize.
                        properties:
                          branch:
                            description: Branch is the Git branch to check out. Omit
                              to use the repo default branch.
                            type: string
                          parent:
                            description: |-
                              Parent is the name of a layer in serverProfileLayers that is applied beneath this profile.
                              Sets SERVER_PROFILE_PARENT.
                            type: string
                          path:
                            description: Path is the subdirectory within the git repo.
                            type: string
                          url:
                            description: URL is the Git HTTPS URL of the server profile
                              repo.
                            type: string
                        type: object
                      serverProfileLayers:
                        description: |-
                          ServerProfileLayers defines additional named profile layers (layered profiles).
                          Keys are the layer names (e.g. "PAZ", "BASE"); env vars use the uppercased key.
                        items:
                          description: |-
                            ServerProfileLayerSpec defines a named server profile layer for layered profiles.
                            Each entry maps to SERVER_PROFILE_<UPPER(NAME)>_URL / _BRANCH / _PATH / _PARENT.
                          properties:
                            branch:
                              description: Branch is the Git branch to check out.
                              type: string
                            name:
                              description: |-
                                Name is the layer identifier (e.g. paz, base, extensions).
                                Env vars use the uppercased name: paz → SERVER_PROFILE_PAZ_URL etc.
                              type: string
                            parent:
                              description: Parent is the name of a deeper layer in
                                serverProfileLayers (sets SERVER_PROFILE_<NAME>_PARENT).
                              type: string
                            path:
                              description: Path is the subdirectory within the git
                                repo.
                              type: string
                            url:
                              description: URL is the Git HTTPS URL of this layer's
                                repo.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                      userBaseDN:
                        description: 'UserBaseDN is the base DN for user data (USER_BASE_DN).
                          Default: dc=example,dc=com.'
                        type: string
                    type: object
                  container:
                    description: Container holds container-level settings such as
                      service wait conditions.
                    properties:
                      waitFor:
                        description: WaitFor is a list of services this container
                          waits for before starting.
                        items:
                          description: WaitForSpec defines a single service dependency
                            that must be reachable before the container starts.
                          properties:
                            application:
                              description: |-
                                Application is the logical name of the Ping product to wait for.
                                Supported values: pingDirectory, pingFederate, pingFederateAdmin, pingFederateEngine,
                                pingAccess, pingAccessAdmin, pingAccessEngine, pingAuthorize, pingAuthorizePAP, pingDataConsole.
                                The operator resolves this to the correct Helm sub-chart service name automatically.
                              type: string
                            service:
                              description: Service is the port/service type to probe
                                (e.g. ldaps, https, ldap).
                              type: string
                            timeoutSeconds:
                              description: 'TimeoutSeconds is how long to wait before
                                giving up. Default: 300.'
                              format: int32
                              type: integer
                          required:
                          - application
                          - service
                          type: object
                        type: array
                    type: object
                  image:
                    description: Image is the container image repository. Omit to
                      use the chart's default.
                    type: string
                  ingress:
                    description: |-
                      Ingress configures the Kubernetes Ingress for the PingAuthorize management interface.
                      Hostname defaults to paz.<spec.domain> when spec.domain is set.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are merged on top of the global annotations
                          for this component.
                        type: object
                      className:
                        description: ClassName overrides the global ingressClassName
                          for this component.
                        type: string
                      enabled:
                        description: |-
                          Enabled controls whether an Ingress resource is created for this component.
                          When nil the value is inherited from spec.ingress.enabled.
                        type: boolean
                      hostname:
                        description: Hostname is the FQDN for this component. Defaults
                          to <prefix>.<spec.domain> when spec.domain is set.
                        type: string
                      tlsSecretRef:
                        description: TLSSecretRef overrides the global TLS secret
                          name for this component.
                        type: string
                    type: object
                  replicas:
                    description: 'Replicas is the desired number of PingAuthorize
                      pods. Default: 1.'
                    format: int32
                    type: integer
                  storageClass:
                    description: StorageClass is the storage class used for PersistentVolumeClaims.
                    type: string
                  storageSize:
                    description: 'StorageSize is the size of the /opt/out PVC. Default:
                      8Gi.'
                    type: string
                  valuesOverride:
                    description: ValuesOverride is merged on top of the base Helm
                      values as raw JSON.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  version:
                    description: Version is the container image tag or full image
                      reference (e.g. 10.3.0.0-edge).
                    type: string
                type: object
              pingAuthorizePAP:
                description: PingAuthorizePAP holds the optional PingAuthorize Policy
                  Editor (PAP) deployment configuration.
                properties:
                  config:
                    description: Config holds PingAuthorizePAP-specific environment
                      variable configuration.
                    properties:
                      clientID:
                        description: ClientID is the OIDC client identifier (PING_CLIENT_ID).
                        type: string
                      enableAPIHTTPCache:
                        description: 'EnableAPIHTTPCache controls HTTP API caching
                          (PING_ENABLE_API_HTTP_CACHE). Default: true.'
                        type: boolean
                      envConfigMapRef:
                        description: EnvConfigMapRef is the name of a ConfigMap with
                          additional env vars to inject.
                        type: string
                      externalBaseURL:
                        description: |-
                          ExternalBaseURL is the external hostname and port for PAP API access (PING_EXTERNAL_BASE_URL).
                          Derived as https://paz-pap.<spec.domain> when spec.domain is set and this is empty.
                        type: string
                      maxHeapSize:
                        description: 'MaxHeapSize is the JVM heap size (MAX_HEAP_SIZE).
                          Default: 384m.'
                        type: string
                      oidcConfigEndpoint:
                        description: OIDCConfigEndpoint is the OIDC provider configuration
                          URL (PING_OIDC_CONFIGURATION_ENDPOINT).
                        type: string
                      policyDBSync:
                        description: PolicyDBSync enables database creation/upgrade
                          mode (PING_POLICY_DB_SYNC).
                        type: boolean
                      serverProfile:
                        description: ServerProfile is the primary server profile for
                          PingAuthorizePAP.
                        properties:
                          branch:
                            description: Branch is the Git branch to check out. Omit
                              to use the repo default branch.
                            type: string
                          parent:
                            description: |-
                              Parent is the name of a layer in serverProfileLayers that is applied beneath this profile.
                              Sets SERVER_PROFILE_PARENT.
                            type: string
                          path:
                            description: Path is the subdirectory within the git repo.
                            type: string
                          url:
                            description: URL is the Git HTTPS URL of the server profile
                              repo.
                            type: string
                        type: object
                      serverProfileLayers:
                        description: ServerProfileLayers defines additional named
                          profile layers (layered profiles).
                        items:
                          description: |-
                            ServerProfileLayerSpec defines a named server profile layer for layered profiles.
                            Each entry maps to SERVER_PROFILE_<UPPER(NAME)>_URL / _BRANCH / _PATH / _PARENT.
                          properties:
                            branch:
                              description: Branch is the Git branch to check out.
                              type: string
                            name:
                              description: |-
                                Name is the layer identifier (e.g. paz, base, extensions).
                                Env vars use the uppercased name: paz → SERVER_PROFILE_PAZ_URL etc.
                              type: string
                            parent:
                              description: Parent is the name of a deeper layer in
                                serverProfileLayers (sets SERVER_PROFILE_<NAME>_PARENT).
                              type: string
                            path:
                              description: Path is the subdirectory within the git
                                repo.
                              type: string
                            url:
                              description: URL is the Git HTTPS URL of this layer's
                                repo.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                      sharedSecretRef:
                        description: SharedSecretRef is the name of a Secret containing
                          DECISION_POINT_SHARED_SECRET for PAZ integration.
                        type: string
                    type: object
                  container:
                    description: Container holds container-level settings such as
                      service wait conditions.
                    properties:
                      waitFor:
                        description: WaitFor is a list of services this container
                          waits for before starting.
                        items:
                          description: WaitForSpec defines a single service dependency
                            that must be reachable before the container starts.
                          properties:
                            application:
                              description: |-
                                Application is the logical name of the Ping product to wait for.
                                Supported values: pingDirectory, pingFederate, pingFederateAdmin, pingFederateEngine,
                                pingAccess, pingAccessAdmin, pingAccessEngine, pingAuthorize, pingAuthorizePAP, pingDataConsole.
                                The operator resolves this to the correct Helm sub-chart service name automatically.
                              type: string
                            service:
                              description: Service is the port/service type to probe
                                (e.g. ldaps, https, ldap).
                              type: string
                            timeoutSeconds:
                              description: 'TimeoutSeconds is how long to wait before
                                giving up. Default: 300.'
                              format: int32
                              type: integer
                          required:
                          - application
                          - service
                          type: object
                        type: array
                    type: object
                  image:
                    description: Image is the container image repository. Omit to
                      use the chart's default.
                    type: string
                  ingress:
                    description: |-
                      Ingress configures the Kubernetes Ingress for the PAP web UI.
                      Hostname defaults to paz-pap.<spec.domain> when spec.domain is set.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are merged on top of the global annotations
                          for this component.
                        type: object
                      className:
                        description: ClassName overrides the global ingressClassName
                          for this component.
                        type: string
                      enabled:
                        description: |-
                          Enabled controls whether an Ingress resource is created for this component.
                          When nil the value is inherited from spec.ingress.enabled.
                        type: boolean
                      hostname:
                        description: Hostname is the FQDN for this component. Defaults
                          to <prefix>.<spec.domain> when spec.domain is set.
                        type: string
                      tlsSecretRef:
                        description: TLSSecretRef overrides the global TLS secret
                          name for this component.
                        type: string
                    type: object
                  valuesOverride:
                    description: ValuesOverride is merged on top of the base Helm
                      values as raw JSON.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  version:
                    description: Version is the container image tag or full image
                      reference.
                    type: string
                type: object
              pingDataConsole:
                description: |-
                  PingDataConsole configures the PingDataConsole web UI.
                  Automatically enabled when pingDirectory is set; set enabled: false to disable.
                properties:
                  enabled:
                    description: |-
                      Enabled controls whether PingDataConsole is deployed.
                      Defaults to true when spec.pingDirectory is configured; set to false to disable.
                    type: boolean
                  image:
                    description: Image is the container image repository. Omit to
                      use the chart's default.
                    type: string
                  ingress:
                    description: |-
                      Ingress configures the Kubernetes Ingress for PingDataConsole.
                      Hostname defaults to pd-console.<spec.domain> when spec.domain is set.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are merged on top of the global annotations
                          for this component.
                        type: object
                      className:
                        description: ClassName overrides the global ingressClassName
                          for this component.
                        type: string
                      enabled:
                        description: |-
                          Enabled controls whether an Ingress resource is created for this component.
                          When nil the value is inherited from spec.ingress.enabled.
                        type: boolean
                      hostname:
                        description: Hostname is the FQDN for this component. Defaults
                          to <prefix>.<spec.domain> when spec.domain is set.
                        type: string
                      tlsSecretRef:
                        description: TLSSecretRef overrides the global TLS secret
                          name for this component.
                        type: string
                    type: object
                  version:
                    description: Version is the container image tag. Omit to use the
                      chart's default tag.
                    type: string
                type: object
              pingDirectory:
                description: PingDirectory holds the optional PingDirectory deployment
                  configuration.
                properties:
                  config:
                    description: Config holds PingDirectory-specific environment variable
                      configuration.
                    properties:
                      adminSecretRef:
                        description: AdminSecretRef is the name of a Secret containing
                          admin credentials.
                        type: string
                      adminUserName:
                        description: 'AdminUserName is the replication admin user
                          (ADMIN_USER_NAME). Default: admin.'
                        type: string
                      envConfigMapRef:
                        description: EnvConfigMapRef is the name of a ConfigMap with
                          additional env vars to inject.
                        type: string
                      failOnDisabledBaseDN:
                        description: 'FailOnDisabledBaseDN fails the container if
                          USER_BASE_DN replication is not enabled. Default: false.'
                        type: boolean
                      fipsModeOn:
                        description: 'FIPSModeOn enables Bouncy Castle FIPS mode (FIPS_MODE_ON).
                          Default: false.'
                        type: boolean
                      httpsPort:
                        description: 'HTTPSPort is the container HTTPS port (HTTPS_PORT).
                          Default: 1443.'
                        format: int32
                        type: integer
                      keystoreSecretRef:
                        description: KeystoreSecretRef is the name of a Secret containing
                          the keystore for TLS.
                        type: string
                      ldapPort:
                        description: 'LDAPPort is the container LDAP port (LDAP_PORT).
                          Default: 1389.'
                        format: int32
                        type: integer
                      ldapsPort:
                        description: 'LDAPSPort is the container LDAPS port (LDAPS_PORT).
                          Default: 1636.'
                        format: int32
                        type: integer
                      makeLdifUsers:
                        description: 'MakeLdifUsers is the number of users to auto-generate
                          on first start (MAKELDIF_USERS). Default: 0.'
                        format: int32
                        type: integer
                      parallelPodManagement:
                        description: 'ParallelPodManagement must be true when StatefulSet
                          uses Parallel podManagementPolicy. Default: false.'
                        type: boolean
                      rebuildOnRestart:
                        description: 'RebuildOnRestart forces replace-profile on every
                          restart (PD_REBUILD_ON_RESTART). Default: false.'
                        type: boolean
                      replicationBaseDNs:
                        description: ReplicationBaseDNs is additional base DNs for
                          replication (REPLICATION_BASE_DNS).
                        type: string
                      replicationPort:
                        description: 'ReplicationPort is the port used for replication
                          communication (REPLICATION_PORT). Default: 8989.'
                        format: int32
                        type: integer
                      retryTimeoutSeconds:
                        description: 'RetryTimeoutSeconds is the timeout for dsreplication
                          operations (RETRY_TIMEOUT_SECONDS). Default: 180.'
                        format: int32
                        type: integer
                      serverProfile:
                        description: ServerProfile is the primary server profile for
                          PingDirectory.
                        properties:
                          branch:
                            description: Branch is the Git branch to check out. Omit
                              to use the repo default branch.
                            type: string
                          parent:
                            description: |-
                              Parent is the name of a layer in serverProfileLayers that is applied beneath this profile.
                              Sets SERVER_PROFILE_PARENT.
                            type: string
                          path:
                            description: Path is the subdirectory within the git repo.
                            type: string
                          url:
                            description: URL is the Git HTTPS URL of the server profile
                              repo.
                            type: string
                        type: object
                      serverProfileLayers:
                        description: |-
                          ServerProfileLayers defines additional named profile layers (layered profiles).
                          Keys are the layer names; env vars use the uppercased key.
                        items:
                          description: |-
                            ServerProfileLayerSpec defines a named server profile layer for layered profiles.
                            Each entry maps to SERVER_PROFILE_<UPPER(NAME)>_URL / _BRANCH / _PATH / _PARENT.
                          properties:
                            branch:
                              description: Branch is the Git branch to check out.
                              type: string
                            name:
                              description: |-
                                Name is the layer identifier (e.g. paz, base, extensions).
                                Env vars use the uppercased name: paz → SERVER_PROFILE_PAZ_URL etc.
                              type: string
                            parent:
                              description: Parent is the name of a deeper layer in
                                serverProfileLayers (sets SERVER_PROFILE_<NAME>_PARENT).
                              type: string
                            path:
                              description: Path is the subdirectory within the git
                                repo.
                              type: string
                            url:
                              description: URL is the Git HTTPS URL of this layer's
                                repo.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                      userBaseDN:
                        description: 'UserBaseDN is the base DN for user data (USER_BASE_DN).
                          Default: dc=example,dc=com.'
                        type: string
                    type: object
                  container:
                    description: Container holds container-level settings such as
                      service wait conditions.
                    properties:
                      waitFor:
                        description: WaitFor is a list of services this container
                          waits for before starting.
                        items:
                          description: WaitForSpec defines a single service dependency
                            that must be reachable before the container starts.
                          properties:
                            application:
                              description: |-
                                Application is the logical name of the Ping product to wait for.
                                Supported values: pingDirectory, pingFederate, pingFederateAdmin, pingFederateEngine,
                                pingAccess, pingAccessAdmin, pingAccessEngine, pingAuthorize, pingAuthorizePAP, pingDataConsole.
                                The operator resolves this to the correct Helm sub-chart service name automatically.
                              type: string
                            service:
                              description: Service is the port/service type to probe
                                (e.g. ldaps, https, ldap).
                              type: string
                            timeoutSeconds:
                              description: 'TimeoutSeconds is how long to wait before
                                giving up. Default: 300.'
                              format: int32
                              type: integer
                          required:
                          - application
                          - service
                          type: object
                        type: array
                    type: object
                  image:
                    description: |-
                      Image is the container image repository (e.g. registry.example.com/org/pingdirectory).
                      Omit to use the chart's default image repository.
                    type: string
                  replicas:
                    description: 'Replicas is the desired number of PingDirectory
                      pods. Default: 1.'
                    format: int32
                    type: integer
                  storageClass:
                    description: StorageClass is the storage class used for PersistentVolumeClaims.
                    type: string
                  storageSize:
                    description: 'StorageSize is the size of the /opt/out PVC. Default:
                      8Gi.'
                    type: string
                  valuesOverride:
                    description: ValuesOverride is merged on top of the base Helm
                      values as raw JSON.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  version:
                    description: |-
                      Version is the container image tag (e.g. 11.0.0.2-edge).
                      Omit to use the chart's default tag.
                    type: string
                type: object
              pingFederate:
                description: PingFederate holds the PingFederate deployment configuration.
                properties:
                  adminIngress:
                    description: |-
                      AdminIngress configures the Kubernetes Ingress for the PingFederate admin console.
                      Hostname defaults to pf-admin.<spec.domain> when spec.domain is set.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are merged on top of the global annotations
                          for this component.
                        type: object
                      className:
                        description: ClassName overrides the global ingressClassName
                          for this component.
                        type: string
                      enabled:
                        description: |-
                          Enabled controls whether an Ingress resource is created for this component.
                          When nil the value is inherited from spec.ingress.enabled.
                        type: boolean
                      hostname:
                        description: Hostname is the FQDN for this component. Defaults
                          to <prefix>.<spec.domain> when spec.domain is set.
                        type: string
                      tlsSecretRef:
                        description: TLSSecretRef overrides the global TLS secret
                          name for this component.
                        type: string
                    type: object
                  config:
                    description: Config holds PingFederate-specific environment variable
                      configuration.
                    properties:
                      adminAPIAuthentication:
                        description: 'AdminAPIAuthentication is the admin API auth
                          mechanism (PF_ADMIN_API_AUTHENTICATION). Default: native.'
                        type: string
                      adminPort:
                        description: 'AdminPort is the HTTPS port for the PingFederate
                          admin console/API (PF_ADMIN_PORT). Default: 9999.'
                        format: int32
                        type: integer
                      adminPublicBaseURL:
                        description: AdminPublicBaseURL is the full base URL of the
                          PF admin console (PF_ADMIN_PUBLIC_BASEURL).
                        type: string
                      adminPublicHostname:
                        description: AdminPublicHostname is the public hostname of
                          the PF admin node (PF_ADMIN_PUBLIC_HOSTNAME).
                        type: string
                      adminSecretRef:
                        description: AdminSecretRef is the name of a Secret containing
                          admin credentials.
                        type: string
                      consoleAuthentication:
                        description: 'ConsoleAuthentication is the admin console auth
                          mechanism (PF_CONSOLE_AUTHENTICATION). Default: native.'
                        type: string
                      consoleEnvironment:
                        description: ConsoleEnvironment is the environment label shown
                          in the admin console UI (PF_CONSOLE_ENV).
                        type: string
                      consoleTitle:
                        description: ConsoleTitle is the title shown in the admin
                          console (PF_CONSOLE_TITLE).
                        type: string
                      enginePort:
                        description: 'EnginePort is the HTTPS port for the PingFederate
                          runtime engine (PF_ENGINE_PORT). Default: 9031.'
                        format: int32
                        type: integer
                      enginePublicHostname:
                        description: EnginePublicHostname is the public hostname of
                          the PF engine node (PF_ENGINE_PUBLIC_HOSTNAME).
                        type: string
                      envConfigMapRef:
                        description: EnvConfigMapRef is the name of a ConfigMap with
                          additional env vars to inject.
                        type: string
                      hsmMode:
                        description: 'HSMMode is the Hardware Security Module mode
                          (HSM_MODE). Default: OFF.'
                        type: string
                      javaRamPercentage:
                        description: 'JavaRAMPercentage is the percentage of container
                          memory for the JVM (JAVA_RAM_PERCENTAGE). Default: 75.0.'
                        type: string
                      ldapSecretRef:
                        description: LDAPSecretRef is the name of a Secret containing
                          PF_LDAP_PASSWORD.
                        type: string
                      ldapType:
                        description: 'LDAPType is the LDAP directory type when using
                          LDAP auth (PF_LDAP_TYPE). Default: PingDirectory.'
                        type: string
                      ldapUsername:
                        description: LDAPUsername is the username for LDAP lookups
                          (PF_LDAP_USERNAME).
                        type: string
                      operationalMode:
                        description: 'OperationalMode is one of STANDALONE, CLUSTERED_CONSOLE,
                          CLUSTERED_ENGINE (OPERATIONAL_MODE). Default: STANDALONE.'
                        type: string
                      pingOneEnvID:
                        description: PingOneEnvID is the PingOne environment ID to
                          connect to (PF_PINGONE_ENV_ID).
                        type: string
                      pingOneRegion:
                        description: PingOneRegion is the region of the PingOne tenant
                          (PF_PINGONE_REGION).
                        type: string
                      provisionerMode:
                        description: 'ProvisionerMode is one of OFF, STANDALONE, FAILOVER
                          (PF_PROVISIONER_MODE). Default: OFF.'
                        type: string
                      provisionerNodeID:
                        description: 'ProvisionerNodeID is the provisioner node ID
                          (PF_PROVISIONER_NODE_ID). Default: 1.'
                        format: int32
                        type: integer
                      serverProfile:
                        description: ServerProfile is the primary server profile for
                          PingFederate.
                        properties:
                          branch:
                            description: Branch is the Git branch to check out. Omit
                              to use the repo default branch.
                            type: string
                          parent:
                            description: |-
                              Parent is the name of a layer in serverProfileLayers that is applied beneath this profile.
                              Sets SERVER_PROFILE_PARENT.
                            type: string
                          path:
                            description: Path is the subdirectory within the git repo.
                            type: string
                          url:
                            description: URL is the Git HTTPS URL of the server profile
                              repo.
                            type: string
                        type: object
                      serverProfileLayers:
                        description: |-
                          ServerProfileLayers defines additional named profile layers (layered profiles).
                          Keys are the layer names; env vars use the uppercased key.
                        items:
                          description: |-
                            ServerProfileLayerSpec defines a named server profile layer for layered profiles.
                            Each entry maps to SERVER_PROFILE_<UPPER(NAME)>_URL / _BRANCH / _PATH / _PARENT.
                          properties:
                            branch:
                              description: Branch is the Git branch to check out.
                              type: string
                            name:
                              description: |-
                                Name is the layer identifier (e.g. paz, base, extensions).
                                Env vars use the uppercased name: paz → SERVER_PROFILE_PAZ_URL etc.
                              type: string
                            parent:
                              description: Parent is the name of a deeper layer in
                                serverProfileLayers (sets SERVER_PROFILE_<NAME>_PARENT).
                              type: string
                            path:
                              description: Path is the subdirectory within the git
                                repo.
                              type: string
                            url:
                              description: URL is the Git HTTPS URL of this layer's
                                repo.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                    type: object
                  container:
                    description: Container holds container-level settings such as
                      service wait conditions.
                    properties:
                      waitFor:
                        description: WaitFor is a list of services this container
                          waits for before starting.
                        items:
                          description: WaitForSpec defines a single service dependency
                            that must be reachable before the container starts.
                          properties:
                            application:
                              description: |-
                                Application is the logical name of the Ping product to wait for.
                                Supported values: pingDirectory, pingFederate, pingFederateAdmin, pingFederateEngine,
                                pingAccess, pingAccessAdmin, pingAccessEngine, pingAuthorize, pingAuthorizePAP, pingDataConsole.
                                The operator resolves this to the correct Helm sub-chart service name automatically.
                              type: string
                            service:
                              description: Service is the port/service type to probe
                                (e.g. ldaps, https, ldap).
                              type: string
                            timeoutSeconds:
                              description: 'TimeoutSeconds is how long to wait before
                                giving up. Default: 300.'
                              format: int32
                              type: integer
                          required:
                          - application
                          - service
                          type: object
                        type: array
                    type: object
                  engineIngress:
                    description: |-
                      EngineIngress configures the Kubernetes Ingress for the PingFederate runtime engine.
                      Hostname defaults to pf.<spec.domain> when spec.domain is set.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are merged on top of the global annotations
                          for this component.
                        type: object
                      className:
                        description: ClassName overrides the global ingressClassName
                          for this component.
                        type: string
                      enabled:
                        description: |-
                          Enabled controls whether an Ingress resource is created for this component.
                          When nil the value is inherited from spec.ingress.enabled.
                        type: boolean
                      hostname:
                        description: Hostname is the FQDN for this component. Defaults
                          to <prefix>.<spec.domain> when spec.domain is set.
                        type: string
                      tlsSecretRef:
                        description: TLSSecretRef overrides the global TLS secret
                          name for this component.
                        type: string
                    type: object
                  image:
                    description: |-
                      Image is the container image repository (e.g. registry.example.com/org/pingfederate).
                      Omit to use the chart's default image repository.
                    type: string
                  replicas:
                    description: 'Replicas is the desired number of PingFederate engine
                      pods. Default: 1.'
                    format: int32
                    type: integer
                  valuesOverride:
                    description: ValuesOverride is merged on top of the base Helm
                      values as raw JSON.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  version:
                    description: |-
                      Version is the container image tag (e.g. 13.0.2-edge).
                      Omit to use the chart's default tag.
                    type: string
                type: object
              targetNamespace:
                description: TargetNamespace is the namespace to deploy into; defaults
                  to metadata.namespace.
                type: string
              tenantId:
                description: TenantID is the PingOne tenant identifier used to name
                  Helm releases.
                type: string
              tier:
                description: 'Tier is one of: development, staging, production.'
                type: string
            required:
            - pingFederate
            - tenantId
            - tier
            type: object
          status:
            description: PingEnvironmentStatus defines the observed state of PingEnvironment.
            properties:
              conditions:
                description: Conditions holds the latest available observations of
                  the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the generation last processed by
                  the reconciler.
                format: int64
                type: integer
              phase:
                description: 'Phase is the current lifecycle phase: Pending, Deploying,
                  Ready, or Failed.'
                type: string
              pingAccessRelease:
                description: PingAccessRelease is the Helm release name for PingAccess.
                type: string
              pingAuthorizePAPRelease:
                description: PingAuthorizePAPRelease is the Helm release name for
                  PingAuthorizePAP.
                type: string
              pingAuthorizeRelease:
                description: PingAuthorizeRelease is the Helm release name for PingAuthorize.
                type: string
              pingDirectoryRelease:
                description: PingDirectoryRelease is the Helm release name for PingDirectory.
                type: string
              pingFederateRelease:
                description: PingFederateRelease is the Helm release name for PingFederate.
                type: string
            required:
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
