---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: pingdirectories.pingone.io
spec:
  group: pingone.io
  names:
    kind: PingDirectory
    listKind: PingDirectoryList
    plural: pingdirectories
    singular: pingdirectory
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.environmentRef
      name: Environment
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PingDirectory is the Schema for the pingdirectories API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PingDirectorySpec defines the desired state of a PingDirectory
              deployment.
            properties:
              config:
                description: Config holds PingDirectory-specific environment variable
                  configuration.
                properties:
                  adminSecretRef:
                    description: AdminSecretRef is the name of a Secret containing
                      admin credentials.
                    type: string
                  adminUserName:
                    description: 'AdminUserName is the replication admin user (ADMIN_USER_NAME).
                      Default: admin.'
                    type: string
                  certificateNickname:
                    description: CertificateNickname is the alias of the certificate
                      to use within the keystore (CERTIFICATE_NICKNAME).
                    type: string
                  encryptionSecretRef:
                    description: EncryptionSecretRef is the name of a Secret containing
                      the encryption passphrase (ENCRYPTION_PASSWORD_FILE).
                    type: string
                  envConfigMapRef:
                    description: EnvConfigMapRef is the name of a ConfigMap with additional
                      env vars to inject.
                    type: string
                  failOnDisabledBaseDN:
                    description: 'FailOnDisabledBaseDN fails the container if USER_BASE_DN
                      replication is not enabled (FAIL_ON_DISABLED_BASE_DN). Default:
                      false.'
                    type: boolean
                  failOnUnsuccessfulRemoveDefunct:
                    description: 'FailOnUnsuccessfulRemoveDefunct fails the container
                      if a previous remove-defunct-server call did not complete successfully
                      (FAIL_ON_UNSUCCESSFUL_REMOVE_DEFUNCT). Default: false.'
                    type: boolean
                  fipsModeOn:
                    description: 'FIPSModeOn enables Bouncy Castle FIPS mode (FIPS_MODE_ON).
                      Default: false.'
                    type: boolean
                  forceDataReimport:
                    description: 'ForceDataReimport forces a backend data export and
                      re-import on restart (PD_FORCE_DATA_REIMPORT). Default: false.'
                    type: boolean
                  httpsPort:
                    description: 'HTTPSPort is the container HTTPS port (HTTPS_PORT).
                      Default: 1443.'
                    format: int32
                    type: integer
                  keystoreSecretRef:
                    description: KeystoreSecretRef is the name of a Secret containing
                      the keystore file and PIN (KEYSTORE_FILE / KEYSTORE_PIN_FILE).
                    type: string
                  keystoreType:
                    description: 'KeystoreType is the format of the keystore (KEYSTORE_TYPE).
                      One of: jks, pkcs12, pem, bcfks.'
                    type: string
                  ldapPort:
                    description: 'LDAPPort is the container LDAP port (LDAP_PORT).
                      Default: 1389.'
                    format: int32
                    type: integer
                  ldapsPort:
                    description: 'LDAPSPort is the container LDAPS port (LDAPS_PORT).
                      Default: 1636.'
                    format: int32
                    type: integer
                  loadBalancingAlgorithmNames:
                    description: LoadBalancingAlgorithmNames is a semicolon-separated
                      list of load-balancing algorithm names (LOAD_BALANCING_ALGORITHM_NAMES).
                    type: string
                  makeLdifUsers:
                    description: 'MakeLdifUsers is the number of users to auto-generate
                      on first start (MAKELDIF_USERS). Default: 0.'
                    format: int32
                    type: integer
                  parallelPodManagement:
                    description: 'ParallelPodManagement must be true when StatefulSet
                      uses Parallel podManagementPolicy. Default: false.'
                    type: boolean
                  rebuildOnRestart:
                    description: 'RebuildOnRestart forces replace-profile on every
                      restart (PD_REBUILD_ON_RESTART). Default: false.'
                    type: boolean
                  replicationBaseDNs:
                    description: ReplicationBaseDNs is additional base DNs for replication
                      (REPLICATION_BASE_DNS).
                    type: string
                  replicationPort:
                    description: 'ReplicationPort is the port used for replication
                      communication (REPLICATION_PORT). Default: 8989.'
                    format: int32
                    type: integer
                  restrictedBaseDNs:
                    description: RestrictedBaseDNs is a semicolon-separated list of
                      base DNs used for entry-balancing configuration (RESTRICTED_BASE_DNS).
                    type: string
                  retryTimeoutSeconds:
                    description: 'RetryTimeoutSeconds is the timeout for dsreplication
                      operations (RETRY_TIMEOUT_SECONDS). Default: 180.'
                    format: int32
                    type: integer
                  serverProfile:
                    description: ServerProfile is the primary server profile for PingDirectory.
                    properties:
                      branch:
                        description: Branch is the Git branch to check out. Omit to
                          use the repo default branch.
                        type: string
                      parent:
                        description: |-
                          Parent is the name of a layer in serverProfileLayers that is applied beneath this profile.
                          Sets SERVER_PROFILE_PARENT.
                        type: string
                      path:
                        description: Path is the subdirectory within the git repo.
                        type: string
                      url:
                        description: URL is the Git HTTPS URL of the server profile
                          repo.
                        type: string
                    type: object
                  serverProfileLayers:
                    description: |-
                      ServerProfileLayers defines additional named profile layers (layered profiles).
                      Keys are the layer names; env vars use the uppercased key.
                    items:
                      description: |-
                        ServerProfileLayerSpec defines a named server profile layer for layered profiles.
                        Each entry maps to SERVER_PROFILE_<UPPER(NAME)>_URL / _BRANCH / _PATH / _PARENT.
                      properties:
                        branch:
                          description: Branch is the Git branch to check out.
                          type: string
                        name:
                          description: |-
                            Name is the layer identifier (e.g. paz, base, extensions).
                            Env vars use the uppercased name: paz → SERVER_PROFILE_PAZ_URL etc.
                          type: string
                        parent:
                          description: Parent is the name of a deeper layer in serverProfileLayers
                            (sets SERVER_PROFILE_<NAME>_PARENT).
                          type: string
                        path:
                          description: Path is the subdirectory within the git repo.
                          type: string
                        url:
                          description: URL is the Git HTTPS URL of this layer's repo.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  skipWaitForDNS:
                    description: 'SkipWaitForDNS skips the DNS readiness check on
                      startup (SKIP_WAIT_FOR_DNS). Default: false.'
                    type: boolean
                  truststoreSecretRef:
                    description: TruststoreSecretRef is the name of a Secret containing
                      the truststore file and PIN (TRUSTSTORE_FILE / TRUSTSTORE_PIN_FILE).
                    type: string
                  truststoreType:
                    description: 'TruststoreType is the format of the truststore (TRUSTSTORE_TYPE).
                      One of: jks, pkcs12, pem, bcfks.'
                    type: string
                  userBaseDN:
                    description: 'UserBaseDN is the base DN for user data (USER_BASE_DN).
                      Default: dc=example,dc=com.'
                    type: string
                type: object
              container:
                description: Container holds container-level settings such as service
                  wait conditions.
                properties:
                  containerSecurityContext:
                    description: ContainerSecurityContext overrides the container-level
                      securityContext for this product.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  includeVolumes:
                    description: |-
                      IncludeVolumes is a list of volume names defined in PingEnvironment.spec.volumes
                      to mount into this product's pod.
                    items:
                      type: string
                    type: array
                  resources:
                    description: |-
                      Resources overrides the CPU/memory requests and limits for this product's containers.
                      Takes precedence over the tier-based defaults and PingEnvironment.spec.resources.
                    properties:
                      limits:
                        additionalProperties:
                          type: string
                        description: Limits are the maximum resources the container
                          may use.
                        type: object
                      requests:
                        additionalProperties:
                          type: string
                        description: Requests are the minimum resources the container
                          requires.
                        type: object
                    type: object
                  securityContext:
                    description: SecurityContext overrides the pod-level securityContext
                      for this product's workload.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  volumeMounts:
                    description: |-
                      VolumeMounts is a list of additional container volumeMounts for this product's workload.
                      Each element is a standard Kubernetes volumeMount spec (name, mountPath, subPath, etc.).
                      Maps directly to the ping-devops sub-chart volumeMounts array.
                    items:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  volumes:
                    description: |-
                      Volumes is a list of additional pod-level volumes for this product's workload.
                      Each element is a standard Kubernetes volume spec (name, configMap, secret, emptyDir, etc.).
                      Maps directly to the ping-devops sub-chart volumes array.
                    items:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                    x-kubernetes-preserve-unknown-fields: true
                  waitFor:
                    description: WaitFor is a list of services this container waits
                      for before starting.
                    items:
                      description: WaitForSpec defines a single service dependency
                        that must be reachable before the container starts.
                      properties:
                        application:
                          description: |-
                            Application is the logical name of the Ping product to wait for.
                            Supported values: pingDirectory, pingFederate, pingFederateAdmin, pingFederateEngine,
                            pingAccess, pingAccessAdmin, pingAccessEngine, pingAuthorize, pingAuthorizePAP, pingDataConsole.
                            The operator resolves this to the correct Helm sub-chart service name automatically.
                          type: string
                        service:
                          description: Service is the port/service type to probe (e.g.
                            ldaps, https, ldap).
                          type: string
                        timeoutSeconds:
                          description: 'TimeoutSeconds is how long to wait before
                            giving up. Default: 300.'
                          format: int32
                          type: integer
                      required:
                      - application
                      - service
                      type: object
                    type: array
                type: object
              environmentRef:
                description: EnvironmentRef is the name of the PingEnvironment CR
                  this product belongs to.
                type: string
              image:
                description: |-
                  Image is the container image repository (e.g. registry.example.com/org/pingdirectory).
                  Omit to use the chart's default image repository.
                type: string
              replicas:
                description: 'Replicas is the desired number of PingDirectory pods.
                  Default: 1.'
                format: int32
                type: integer
              service:
                description: |-
                  Service holds Kubernetes Service customisation for this product.
                  Annotations are merged on top of spec.services.annotations.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are merged on top of the global service
                      annotations for this component.
                    type: object
                type: object
              storageClass:
                description: StorageClass is the storage class used for PersistentVolumeClaims.
                type: string
              storageSize:
                description: 'StorageSize is the size of the /opt/out PVC. Default:
                  8Gi.'
                type: string
              valuesOverride:
                description: ValuesOverride is merged on top of the base Helm values
                  as raw JSON.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              version:
                description: |-
                  Version is the container image tag (e.g. 11.0.0.2-edge).
                  Omit to use the chart's default tag.
                type: string
            type: object
          status:
            description: PingDirectoryStatus describes the observed state of a PingDirectory
              resource.
            properties:
              conditions:
                description: Conditions holds the latest available observations of
                  the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the generation last processed by
                  the reconciler.
                format: int64
                type: integer
              phase:
                description: 'Phase is the current lifecycle phase: Pending, Ready,
                  or Failed.'
                type: string
              release:
                description: Release is the Helm release name that manages this product.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
